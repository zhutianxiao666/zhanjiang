import Vue from 'vue';
import Vuex from 'vuex';
Vue.use(Vuex);
const state = {};
const getters = {};
const actions = {};
const modules = {};
export default new Vuex.Store({
    state,
    getters,
    actions,
    modules
});
